import fetchMentors from '../../../actions/salesExecProfiles/fetchMentors'

const getRoleBasedUserAndCount = (userId, userRole, fetchLimits) => {
  fetchMentors(fetchLimits)
}

export default getRoleBasedUserAndCount
